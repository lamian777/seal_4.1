# 项目规则

- 这是一个单页前端工具，默认保持 `index.html` 作为主要入口，不要轻易改成复杂框架或拆成多层结构。
- 所有位置、尺寸和坐标统一按毫米处理，换算规则保持一致，不要随意更改。
- 改动印章相关功能时，必须同时检查屏幕预览和最终导出结果，不能只看其中一边。
- 每次改完，至少用一个透明 PNG 印章和一个多页 PDF 做人工验证，确认上传、预览、拖动和导出都正常。
- 导出逻辑必须保留现有结果行为，特别是“最终 PDF 变成图片式输出”这一点，除非明确要求改成可编辑输出。
- 本地缓存使用浏览器存储，修改相关数据结构时要考虑旧数据兼容，不要随意改键名。
- 保持中文文案和当前桌面式布局风格，避免为了重构破坏用户习惯。
- 默认不要新增依赖或构建流程；只有在确实解决问题时才考虑引入。
- 使用外部 CDN 的地方要考虑网络失败场景，避免页面因外部资源不可用而直接失效。
- 不要把临时调试代码、控制台输出或一次性验证逻辑留在最终文件里。
- 任何较大的改动后，都先在浏览器里实际打开并检查一遍，再对外说明完成。
